---
phase: 1
plan: 3
wave: 2
---

# Plan 1.3: Update API Endpoints

## Objective
The Dashboard API endpoints in `routes/productRoutes.js` (or `backend/routes/products.js`) must be updated to correctly return products explicitly owned or currently held by the user querying, mapping directly to our new `_id` based explicit fields. 

## Context
- .gsd/SPEC.md
- e:\TraceX1\backend\routes\products.js (or equivalent)
- e:\TraceX1\backend\controllers\productController.js (if exists)

## Tasks

<task type="auto">
  <name>Refactor Dashboard Fetching Logic</name>
  <files>e:\TraceX1\backend\routes\</files>
  <action>
    - Identify the product fetch endpoints (`GET /api/products/owner/:address` or similar, `/api/products/`).
    - Adjust queries so that depending on the user's role (Manufacturer, Distributor, Retailer, Consumer), they only fetch products where their user `_id` matches `manufacturer_id`, `distributor_id`, `retailer_id`, or `consumer_id` on the product.
    - Validate that users only see appropriate data (e.g., retailers do not see products from other retailers).
  </action>
  <verify>npm run test || echo "Check routes"</verify>
  <done>API routes correctly restrict and supply data based on explicit database IDs and exact supply chain states.</done>
</task>

## Success Criteria
- [ ] Backend product routes query on the explicit MongoDB `_id` fields appropriately.
- [ ] The dashboard reflects accurate isolated testing when verified on the frontend.
