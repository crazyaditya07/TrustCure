---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Database Schema Modernization

## Objective
Update the `Product.js` and `User.js` schemas to correctly lock a medical supply chain lifecycle. The product model will track explicit history through the supply chain with role-bound `ObjectId` bindings (`manufacturer_id`, `distributor_id`, `retailer_id`, `consumer_id`), replacing the generic unstructured approach.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- e:\TrustCure1\backend\models\Product.js
- e:\TrustCure1\backend\models\User.js

## Tasks

<task type="auto">
  <name>Refactor User Schema</name>
  <files>e:\TrustCure1\backend\models\User.js</files>
  <action>
    - Ensure `walletAddress` is marked as `required: true` instead of just `sparse: true` or update validation logic to enforce it since all users must use MetaMask. Ensure the user registration routes in `backend/routes/auth.js` can handle this property.
    - If needed, keep backward compatibility while adding enforcement.
  </action>
  <verify>grep -n "walletAddress:" e:\TrustCure1\backend\models\User.js -A 5</verify>
  <done>The wallet address is structurally enforced for all users.</done>
</task>

<task type="auto">
  <name>Refactor Product Schema</name>
  <files>e:\TrustCure1\backend\models\Product.js</files>
  <action>
    - Add explicit `ObjectId` reference fields for every step of the supply chain:
      `manufacturer_id`, `distributor_id`, `retailer_id`, `consumer_id`. Connect them to `ref: 'User'`.
    - Keep the `currentStage` Enum: `['Created', 'Manufactured', 'InDistribution', 'InRetail', 'Sold']`.
    - Modify the `getVisibleHistory` method to use explicit references rather than generic role checks if appropriate, or ensure it correctly builds visibility around these explicit keys.
  </action>
  <verify>npx eslint e:\TrustCure1\backend\models\Product.js</verify>
  <done>Product.js contains `manufacturer_id`, `distributor_id`, `retailer_id`, and `consumer_id` explicitly with references to the User model, and the code passes linting.</done>
</task>

## Success Criteria
- [ ] User.js has walletAddress properly required/handled.
- [ ] Product.js explicitly defines the supply chain lineage fields.
